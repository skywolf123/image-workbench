# image-workbench

浏览器端 AI 图像生成工作台。本文件是领域通用语言（glossary），只收录本领域特有的概念。

## Language

### 图像生成

**任务 (Task)**：
一次图像生成的完整记录：提示词、参数、状态，以及对输入/输出图片的引用。
_Avoid_: 请求、作业

**图片 (Image)**：
内容寻址（id 即内容哈希）的图片字节。被任务、会话与输入草稿引用；引用全部消失时才是可清理的。
_Avoid_: 文件、附件

### 多设备同步

**同步 (Sync)**：
把本机数据与成员空间的活跃数据集双向对齐的过程，语义是合并不替换。
_Avoid_: 备份（旧的单向推送模型，已废弃）、镜像

**成员码 (Member Code)**：
用户持有的标识符，命名服务端上的一个成员空间。只做命名空间命名，不承担认证。
_Avoid_: 账号、用户 ID、密码、Token

**成员空间 (Member Space)**：
服务端上以成员码命名的全部数据：任务、图片与回收站。会话、收藏夹与设置只存在于本机，不属于成员空间。
_Avoid_: 账户、Bucket

**活跃数据集**：
成员空间中当前生效的任务与图片；不含回收站内容。
_Avoid_: 快照（指旧备份机制的整份状态）

**回收站 (Trash)**：
成员空间内暂存已删除任务的服务端区域，任务级（图片字节跟随任务保留）。还原使任务回到活跃数据集；清空才真正删除。
_Avoid_: 垃圾桶、删除队列

### 请求转发

**网关 (Gateway)**：
部署服务端提供的转发通道：以服务端持有的 Key 把请求转发到上游 API。
_Avoid_: 代理（专指上游代理）、中转

**上游代理 (API Proxy)**：
上游项目自带的转发功能：透传前端自带的 Key，语义保持与上游一致。
_Avoid_: 网关
