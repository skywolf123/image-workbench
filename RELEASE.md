## v0.7.12-iw11（2026-09-18）

### 修复
- PC 端「一键新建」按钮垂直对齐：之前因 wrapper 使用 `items-end`，按钮悬在输入框中段；改为 `items-center` 让按钮垂直居中于输入框中央，与底部按钮区 attach/submit 按钮的视觉风格一致。

## v0.7.12-iw10（2026-09-18）

### 新增
- 新增「一键新建」按钮：清空当前提示词、参考图和遮罩，带二次确认弹窗。PC 端 `+` 号按钮位于提示词输入框左侧，移动端位于底部按钮区最左侧，已是全新状态时点击会提示「当前已是全新状态」。

### 变更
- 移动端底部按钮区顺序调整为「新建 / 生成图像 / 添加参考图」，其中「添加参考图」图标与 PC 端统一为回形针样式。

## v0.7.12-iw09（2026-09-18）

### 变更
- 重构「高级编辑」组件：主组件从 1335 行降到 1070 行，拆分出 4 个独立 UI panel（History / Tool / TextStyle / MaskStyle / Finish）、types / utils / useIsMobileDevice 子文件，模块边界清晰。
- 抽 `isBaseImage(o)` helper，消除 9 处 `getEditorKind(...) === 'base-image'` 重复判断。
- 移除 `getEditorKind` / `getMaskShapeTypeFromObject` 中冗余的 `typeof object !== 'object'` 防御性检查。

## v0.7.12-iw08（2026-09-18）

### 新增
- 「高级编辑」：参考项目 gpt_image_playground 的画布编辑器迁入。在 Lightbox（参考图与结果图）与 MaskEditorModal 工具栏提供入口，支持涂抹填色、文字、贴图、水平/垂直翻转、撤销重做；编辑结果可替换当前参考图或追加为新参考图。
- `fabric@^7.3.1` 新增依赖。

### 变更
- 移除外层入口的 DetailModal「高级编辑」按钮（仅保留 Lightbox 与 MaskEditorModal 两处入口）。
- 移除 v0.7.12-iw06 引入的 PWA 更新提示横幅组件（横幅出现条件受限，在常规使用中难以触发）。

## v0.7.12-iw07（2026-09-17）

### 新增
- 「高级编辑」：参考项目 gpt_image_playground 的画布编辑器迁入。在 Lightbox（参考图与结果图）、DetailModal 与 MaskEditorModal 工具栏提供入口，支持涂抹填色、文字、贴图、水平/垂直翻转、撤销重做；编辑结果可替换当前参考图或追加为新参考图。
- `fabric@^7.3.1` 新增依赖。

### 变更
- 移除外层入口的 DetailModal「高级编辑」按钮（仅保留 Lightbox 与 MaskEditorModal 两处入口）。

## v0.7.12-iw06（2025-01-21）

### 新增
- PWA 更新提示：检测到新版本时自动在顶部显示横幅，点击「立即更新」按钮平滑升级到新版本。
- 下拉刷新：在页面顶部下拉可刷新整个应用，适用于 iPad 等设备在 PWA 模式下无浏览器工具栏的场景。

## v0.7.12-iw05（2025-01-21）

### 新增
- 提示词模板缩略图优化：列表加载速度提升 95%。

## v0.7.12-iw04（2026-09-17）

### 新增
- 多设备同步：任务与图片在设备和服务器之间自动同步，每次操作后、每分钟与页面关闭前各一次，多台设备共用一个成员码空间。
- 服务端回收站：删除的任务先进入成员空间的回收站，其他设备同步后同样删除；可在设置的「同步」标签里逐条还原，或输入成员码二次确认后彻底清空。
- 后端网关路由：前端没填 API Key 的请求自动走同源 `/api/gateway/`，由服务端注入 `GATEWAY_API_KEY` 后转发到 `GATEWAY_API_URL`，Key 与上游地址都不进前端产物。
- 同步断线自愈：同步失败自动重试；服务器数据意外丢失时，设备会察觉并把本地内容重新灌回服务器，不会误删本地任务。
- 清除数据拆成两种语义：「清空所选数据」只影响本机并退出同步，新增的「删除所有设备的数据」会把全部任务同步进服务器回收站。

### 变更
- 原「自动备份」机制重做为完全同步：由服务器按任务合并（以较新的一方为准），多台设备可以放心地各自增删任务，不再是用服务器快照替换本地。
- 同步范围收窄为任务与图片：收藏夹、Agent 会话与设置只保存在本机，不再上传。
- API 转发与网关彻底分离：`/api-proxy/` 回归上游的纯转发语义，不再注入任何 Key；注入 Key 的网关走独立的 `/api/gateway/`。
- 成员码切换改为合并语义：本地内容与目标空间的内容自动取并集，不再有「覆盖本地」的确认弹窗。
- 运行中的任务只在本地存在，不参与同步；从服务器拉到还在运行的任务会标记为已中断，不会留下永远转圈的僵尸任务。

### 修复
- 旧备份机制在多设备下相互覆盖、自动上传偶发失败或明显延迟的问题，随同步机制重做一并解决。

### 破坏性变更
- `DEFAULT_API_KEY` / `DEFAULT_API_KEY_FILE` 不再生效，后端持有 Key 改用 `GATEWAY_API_KEY` / `GATEWAY_API_KEY_FILE` 并需同时配置 `GATEWAY_API_URL`；继续设置旧变量服务端会打印迁移警告。
- 旧备份接口 `/api/backup/*` 退役，替换为 `/api/sync/*`；`/data` 里旧格式的备份数据不会被自动迁移，升级后各设备重新与服务器同步即可。
- 「备份」设置标签替换为「同步」；旧版本保存的备份配置（成员码）会自动迁移，无需手动处理。

## v0.7.12-iw03（2026-09-16）

### 新增
- 后端兜底配置：部署方可在服务端持有 API Key 与上游地址，前端没有配置时由服务端补上；前端配置了一律以前端为准。
- 新增 `HIDE_API_SETTINGS` 开关，隐藏设置页的「API 配置」标签，使用者无需配置即可直接生成图片。
- 新增 `LOCK_PRESET_KEY` 开关，锁定预置配置的 API Key 字段，与上游的 `LOCK_PRESET_CONFIG_PARAMS` 互补。
- 自动备份到服务器：图片、任务、收藏与 Agent 会话在后台自动上传，浏览器存储被清空后填入成员码即可取回。
- 成员空间：一个成员码对应服务器上的一个数据空间，同组成员共用一个码即可共享备份。
- 主动向浏览器申请持久化存储权限，并改用本项目的存储命名，降低数据被浏览器自动清理的概率。

### 变更
- 版本号改为「所基于的上游版本 + 本项目发布序号」，便于辨认本 fork 相对上游的位置。
- 工具链由 npm 迁移到 pnpm。

### 修复
- 上游使用 https 地址时服务端会整个进程崩溃，前端表现为生成失败并提示「请求立即失败」。已改为按上游协议选择 http 或 https 模块。
- 打开设置页时只重置了备份服务探测状态却未重新探测，导致「备份」标签不显示。
- 同一上游版本下的新发布无法触发更新提示，版本比较此前忽略了 fork 序号。
- 设置页的备份页改为卡片式布局，与其它设置页保持一致。
- 界面左上角的图标与标题改为本项目名称。
