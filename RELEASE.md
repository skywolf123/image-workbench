## v0.7.12-iw01（2026-09-16）

### 新增
- 后端兜底配置：部署方可在服务端持有 API Key 与上游地址，前端没配置时由服务端补上；前端配置了则一律以前端为准。
- `HIDE_API_SETTINGS` 开关，隐藏设置页的「API 配置」标签，让用户无需配置即可使用。
- `LOCK_PRESET_KEY` 开关，锁定预置配置的 API Key 字段，与上游的 `LOCK_PRESET_CONFIG_PARAMS` 互补。
- 自动备份到服务器：图片、任务、收藏与 Agent 会话在后台自动上传，浏览器存储被清空后填入成员码即可取回。
- 成员空间：一个成员码对应服务器上的一个数据空间，同组成员共用一个码。
- 浏览器存储持久化申请与存储命名隔离，降低数据被浏览器自动清理的概率。

### 变更
- 版本号改为「上游主版本 + 本项目序号」的形式，便于辨认本 fork 相对上游的位置。
- 工具链由 npm 迁移到 pnpm。

### 修复
- 打开设置页时只重置了备份服务探测状态却未重新探测，导致「备份」标签不显示。
- 设置页的备份页改为卡片式布局，与其它设置页保持一致。
- 界面左上角的图标与标题改为本项目名称。
